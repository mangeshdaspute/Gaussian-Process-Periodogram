# GP Periodogram — Python dependencies
# Install with: pip install -r requirements.txt
#
# Note: 'mlp' is a local module included in this repository.
#       It is NOT available on PyPI and must not be listed here.
# Note: 'celerite' (v0.x) is required, NOT 'celerite2'.

numpy>=1.21
pandas>=1.3
matplotlib>=3.4
scipy>=1.7
celerite>=0.4.0
joblib>=1.0
tqdm>=4.60
astropy>=5.0
